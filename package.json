{
  "name": "chris_fitness",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-dotenv": "^0.1.3",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4",
    "cohere-apilib": "./Cohere+API-TS_GENERIC_LIB/TS_GENERIC_LIB"
  },
  "scripts": {
    "build": "if [ \"$WEBPACK_FALLBACK\" = \"stream\" ]; then npm run build-with-polyfill; else npm run build-without-polyfill; fi",
    "build-with-polyfill": "webpack --config webpack.config.js",
    "build-without-polyfill": "webpack --config webpack.config.no-polyfill.js",
    "start": "react-scripts start",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
